export const metadata = {
  title: "Masuk",
  description: "Masuk SchoolWall",
};

const LoginPageLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="bg-slate-100 px-4">{children}</div>;
};

export default LoginPageLayout;
